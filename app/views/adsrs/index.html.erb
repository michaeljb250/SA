<head>
<title>Typing Animation</title>
<link rel="stylesheet">
</head>
<body>
<style>
  /* CSS for the keyboard element */
  #keyboard {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    align-items: center;
    margin: 20px 0;
    font-family: 'Courier New', Courier, monospace;
  }

  /* CSS for the keyboard buttons */
  #keyboard button {
    width: 50px;
    height: 100px;
    margin: 5px;
    border: none;
    border-radius: 5px;
    background-color: #eee;
    color: #333;
    font-size: 18px;
    font-weight: bold;
    box-shadow: 1px 1px 3px rgba(0,0,0,0.3);
    transition: all 0.1s ease;
    font-family: 'Courier New', Courier, monospace;
  }

  /* CSS for the pressed keyboard buttons */
  #keyboard button:active {
    transform: translateY(2px);
    box-shadow: 0px 0px 2px rgba(0,0,0,0.3);
    background-color: #ddd;
    font-family: 'Courier New', Courier, monospace;
  }

  input[type="range"] {
  -webkit-appearance: none;
  height: 8px;
  background-color: #ddd;
  outline: none;
  border-radius: 10px;
  box-shadow: inset 0 0 5px rgba(0, 0, 0, 0.2);
  width: 200px;
}

input[type="range"]::-webkit-slider-thumb {
  -webkit-appearance: none;
  height: 20px;
  width: 20px;
  border-radius: 50%;
  background-color: black;
  margin-top: -6px;
  box-shadow: 0 0 5px rgba(0, 0, 0, 0.2);
}

input[type="range"]::-moz-range-thumb {
  height: 20px;
  width: 20px;
  border-radius: 50%;
  background-color: #2196f3;
  margin-top: -6px;
  box-shadow: 0 0 5px rgba(0, 0, 0, 0.2);
}
</style>
<script src="https://unpkg.com/@hotwired/stimulus@3.0.0/dist/stimulus.umd.js"></script>
   <h1>Tone.js Audio Player</h1>
   <div class="conter">
    <h1 id="text"></h1>
    <div class="button-container">
      <button id="back-btn">Back</button>
      <button id="next-btn">Next</button>
    </div>
    <div id="textt-box">
      <p id="textt"></p>
    </div>
  <div id="synth-container">
    <div data-controller="adsr">
    <div id="keyboard">
    </div>
  </div>
  <div id="controls">
    <div class="control">
      <label>Attack:</label>
      <input type="range" min="0" max="1" step="0.01" value="0" id="attack-slider">
    </div>
    <div class="control">
      <label>Decay:</label>
      <input type="range" min="0" max="1" step="0.01" value="0.5" id="decay-slider">
    </div>
    <div class="control">
      <label>Sustain:</label>
      <input type="range" min="0" max="1" step="0.01" value="0.5" id="sustain-slider">
    </div>
    <div class="control">
      <label>Release:</label>
      <input type="range" min="0" max="1" step="0.01" value="0.5" id="release-slider">
    </div>
  </div>
  </div>
 </div>
</div>
</body>
