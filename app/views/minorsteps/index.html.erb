<html lang="en">
<head>
<!-- Google tag (gtag.js) -->
<script async src="https://www.googletagmanager.com/gtag/js?id=G-RH29JV6H75"></script>
<script>
  window.dataLayer = window.dataLayer || [];
  function gtag(){dataLayer.push(arguments);}
  gtag('js', new Date());

  gtag('config', 'G-RH29JV6H75');
</script>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Minor Scale</title>
    <style>
        /* ... (CSS styles remain unchanged) */
    </style>
</head>
<style>
body {
    # background-color: #ffffff;
    # color: #333333;
    # font-family: 'Arial', sans-serif;
    # display: flex;
    # flex-direction: column;
    # align-items: center;
    # justify-content: center;
    # height: 100vh;
    # margin: 0;
    # text-align: center;
    font-family: monospace, Arial, sans-serif;
    text-align: center;
    background-color: #f5f5f5;
    margin: 0;
    padding: 0;
}

#title {
    font-size: 2.5em;
    margin-bottom: 20px;
    color: #4285f4; /* Google Blue */
}

#description {
    # max-width: 600px;
    text-align: center;
    margin-bottom: 30px;
    # color: #555555;
}

#buttons-container {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
}

button {
    background-color: #4285f4; /* Google Blue */
    border: none;
    color: #ffffff;
    padding: 15px 30px;
    font-size: 1.2em;
    margin: 10px;
    cursor: pointer;
    transition: background-color 0.3s ease, transform 0.2s ease;
    border-radius: 5px;
}

button:hover {
    background-color: #1c4dff; /* Slightly darker blue */
    transform: scale(1.05);
}

button:last-child {
    margin-right: 0;
}

button[data-interval="H"] {
    margin-right: 5px;
}

button[data-interval="W"] {
    margin-right: 15px;
}

p {
    font-size: 18px;
    line-height: 1.5;
    color: black;
    margin-bottom: 30px;
}
</style>
<body>

<div data-controller="minorstep">
    <div id="title">Minor Scale</div>
    <div id="description">Click the buttons below to play notes from the C minor scale. The gaps between buttons represent half or whole steps.
  </div>
  <div id="description">
Lets apply the pattern of whole and half steps to build the scale. Let's use the example of C minor:
 Start with C (root), one Whole step to D, one Half step to E♭, one Whole step to F, Whole step to G, Half step to A♭ and a Whole step to B♭</div>
    <div id="buttons-container"></div>
</body>
</div>
</html>
